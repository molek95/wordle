import { Routes } from "@angular/router";
import { GameConfigComponent } from "./game-config.component";

export const GAME_CONFIG_ROUTES: Routes = [
    {
      path: '',
      component: GameConfigComponent
    }
  ];